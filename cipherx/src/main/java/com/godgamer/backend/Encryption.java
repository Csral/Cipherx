package com.godgamer.backend;

public class Encryption {
    // if while clicking run it opens UI, then either create test units or 
    // go to pom.xml and change the main class in the configuration to desired class
    // e.g. <mainClass>com.godgamer.App</mainClass> --> <mainClass>com.godgamer.backend.Encryption</mainClass>
}
